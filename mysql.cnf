### Step 2: Create MySQL Configuration File

Create a MySQL configuration file to ensure proper settings:

[client]
default-character-set=utf8mb4

[mysql]
default-character-set=utf8mb4

[mysqld]
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci
